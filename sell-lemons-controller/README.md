# Sell Lemons Controller 4.0.19-dev

В 4.0.19 убран безусловный rebirth по готовой награде. Перед действиями контроллер сравнивает текущую награду, продолжение дохода, параллельный проход доступных стендов и покупку UpgradeStack до каждого доступного уровня (+5, +25, +100, MAX). Если подготовка выгоднее, он покупает нужные уровни Stack и улучшает стенды; если большая награда уже заработана, может сразу выполнить rebirth. Готовая эволюция и защищённое завершение Halo имеют приоритет. Полная стройка и обязательный проход всех стендов не нужны.

Сравнение использует текущий доход с учётом постоянных бонусов, точные цены/число уровней, общие деньги на весь проход, траты инвесторов и их влияние на доход, время действий и период выплаты. Это ограниченный прогноз темпа роста инвесторов, а не гарантия глобально оптимального пути. После каждой подтверждённой покупки и непосредственно перед rebirth решение пересчитывается. Недоступный доход не разрешает слепой сброс: обычные доступные покупки продолжаются.

Для экономически выбранного UpgradeStack прежний общий лимит Powers в 5% не блокирует полезный MAX: учитывается вся реальная стоимость и потеря инвесторского множителя. Остальные Powers сохраняют прежний бюджет. Каждая покупка проверяет свежий кошелёк, цену, уровень, эпоху и фактическое подтверждение; неподтверждённый запрос блокирует повтор. Историческое имя настройки `RepeatRebirth` сохранено для совместимости, но теперь включает адаптивный выбор, а не спам. F7 содержит рассмотренные варианты, расходы, уровни, доход, оценки и выбранное действие.

Локально проверены цепочка до MAX с двумя параллельными стендами, +100 без доступного MAX, пропуск ненужной подготовки, отказ от слабого rebirth, смена решения перед отправкой, готовая эволюция и финал Halo. Сборка 4.0.19 ещё не проверена в живом Roblox.

В 4.0.17 исправлен сбой запуска `attempt to index nil with 'new'` из отчёта H82/E4. GoalPlanner теперь получает библиотеку больших чисел из уже инициализированного Core. Раньше он сохранял ещё не созданную ссылку ClientContext, и первая оценка rebirth останавливала работу. Добавлены проверки настоящего порядка загрузки, старта на данных этого отчёта и повторного запуска F6. Исправление и весь набор тестов проверены локально; запуск 4.0.17 в живом Roblox ещё не проверен.

В 4.0.16 появился отдельный выбор ближайшей цели перед работой с базой. Готовая эволюция проверяется каждые 0,5 секунды и при BOOT до планшета/Powers/стендов; она может прервать восстановление после rebirth. Запас 400 теперь относится только к планшету, а не к эволюции. Дешёвая очередная постройка и неизвестная цена Buy сами по себе больше не задерживают готовую эволюцию.

Rebirth ради следующей эволюции сравнивается с продолжением: точный порог производства вычисляется из проверенной формулы игры, а после сброса моделируется только необходимая начальная часть уже наблюдённого маршрута. В прогнозе оплачиваются покупки и затраченное на действия время; полный прежний доход не выдаётся сразу после сброса. Реальный доход включает постоянные улучшения и донат, без повторного умножения. Полная база и завершённый проход стендов не обязательны для этого решения. При отсутствии наблюдений используются прежние проверки; ускорение не объявляется доказанным без данных. Начальное подключение посреди ручного прохода не считается наблюдением с нуля.

Если кошелёк покрывает все оставшиеся покупки по проверенной таблице игры и состоянию базы, цель меняется на завершение Halo, а бюджет защищается от расходов на стенды. Защита подтверждённого FINISH и ограниченной попытки закончить текущую базу сохранена. В панели отображается текущая цель, в F7 — GOAL_ROUTE, оценки времени и причина отсутствия расчёта.

Проверены локально: BOOT с готовой эволюцией и бонусами 0/10/264, короткий rebirth при сильном стартовом доходе, отказ от него при близкой эволюции, точные формулы при огромных числах, стоимость оставшейся базы и запреты повторных/неподтверждённых действий. Сборка 4.0.16 ещё не проверена в живом Roblox.

В 4.0.15 исправлена преждевременная отмена попытки завершить текущую базу. В игровом журнале H35/E6 прогноз отвёл на финал 17,32 секунды, но первый отрицательный пересчёт через 2,09 секунды снял защиту и привёл к эволюции. Теперь одна отрицательная оценка не отменяет попытку. Для досрочной отмены нужны разные свежие расчёты, не менее 5 секунд отрицательных наблюдений на той же постройке и подтверждённая длительная нехватка денег после полных проходов стендов. Стройка и улучшения продолжаются.

Срок попытки остаётся исходным: новые покупки, +1 и повторные проверки перед отправкой сброса не продлевают его. Изменение цели или восстановившийся прогноз очищают отрицательные наблюдения. Изначально далёкий Halo без положительного прогноза по-прежнему не задерживает готовую эволюцию. Подтверждённый FINISH и деньги на весь остаток защищены отдельно. Прогноз будущих бонусов не объявляется гарантией завершения.

В F7 добавлен отдельный журнал последних 24 решений об эволюции: цель, деньги, доход, остаток, прогноз времени и причина отмены попытки. Он сохраняется через сбросы и перезагрузку контроллера. Пятисекундное накопление, +100, прямые действия и одновременное развитие базы/стендов сохранены. Регрессия и реальный путь прямого сброса проверены локальными тестами; игровой запуск 4.0.15 пока не проверен.

В 4.0.14 накопление ограничено **5 секундами** на одну цель. Покупка выполняется сразу, как хватает денег: найден и исправлен отказ проверки прямого Buy из-за отсутствующей GUI-кнопки. Раньше деньги уже лежали на балансе, но WAIT_CASH ждал конца 30-секундного срока. Теперь проверка прямых котировок использует цену и доступность из адаптера, а сам адаптер по-прежнему проверяет состояние и подтверждение покупки.

Смена или завершение цели освобождает накопление, даже если денег на старую цену ещё нет. Доступность покупки проверяется заново без открытия меню; исчерпанные Buy uses не считаются нехваткой денег. Старый сохранённый 30-секундный бюджет сокращается до 5 секунд с учётом уже прошедшего времени. Истёкшее ожидание не запускается повторно на ту же цель. После срока возобновляется экономическое сравнение; сам таймер не является поводом для rebirth.

Ошибка воспроизведена на архивной 4.0.13 реальными модулями чтения, проверки доступности, накопления и прямой покупки в офлайн-среде. Полный набор локальных проверок проходит. Игровой запуск 4.0.14 пока не проверен. Прямые сбросы, +100 и совместная покупка базы со стендами сохранены.

В 4.0.13 исправлено бесконечное откладывание rebirth с причиной BUILD_TARGET_SOON. В отчёте H29/E1 одна Public Citrus Works удерживала 135 слабых проходов: скрипт обещал скорую покупку, продолжал тратить деньги на стенды, а срок ожидания вообще не запускался. Теперь короткое ожидание запускает единый бюджет для конкретной постройки и действительно сохраняет деньги в WAIT_CASH. Давность уже наблюдаемого препятствия учитывается; новые котировки и +1 не продлевают срок.

После исчерпания бюджета выполняется прежнее сравнение дохода после полного восстановления базы с продолжением роста, без фиксированного множителя инвесторов. Доступная постройка, близкая эволюция, восстановление после сброса и достижимый FINISH сохраняют защиту. Ожидание крупного бонуса использует тот же бюджет. Готовая эволюция проверяется и во время накопления. F7 теперь показывает BUILD_WAIT с оставшимся временем.

Сценарий из отчёта воспроизведён на архивной 4.0.12 и проходит на 4.0.13 вместе с полной офлайн-проверкой. Прямые Evolution/Halo из 4.0.12 подтверждены присланным игровым журналом; исправление ожидания rebirth в 4.0.13 пока проверено только локально. +100 и параллельная стройка/стенды сохранены; во время выбранного короткого накопления деньги защищены от расходования на стенды.

В 4.0.12 Evolution и Halo выполняются напрямую: без открытия меню, подтверждающих кликов и движения мыши. Обычные вызовы проверены по ранее выгруженному коду игры v6916: Evolve получает nil, Ascend вызывается без аргументов. Перед использованием сравниваются байткоды обработчиков текущего клиента. Если они изменились, остаётся прежняя защищённая GUI-транзакция; причина видна в журнале.

Готовность, запас инвесторов после Evolution и защита достижимого финала проверяются заново перед отправкой. Успех требует точного изменения пяти счётчиков своей базы. Один ответ сервера без изменения прогресса не считается успехом; неопределённый запрос не повторяется. После подтверждения выполняется полная очистка старых наблюдений и восстановление BOOT → POWERS → стройка.

Исправлен автоматический скан: теперь он читает точные пути модулей, не обходит тысячи моделей. Пропущенные обработчики отмечаются как partial, а старые ошибочно complete сканы 4.0.11 повторяются при перезагрузке с сохранением снимков кнопок. Прямые отправка и подтверждённый результат записываются по одному разу в отдельный раздел F7. Дополнительную диагностическую команду запускать не требуется.

В присланном журнале 4.0.11 подтверждены две эволюции и один Halo, без текущей ошибки; на момент снимка новая эволюция готова на 53%. Это проверка прошлой версии. Для 4.0.12 пройдены локальные регрессионные и интеграционные тесты; живая проверка новых прямых Evolution/Halo ещё не выполнена. +100 и параллельная стройка/прокачка стендов сохранены.

В 4.0.11 исправлена отмена эволюции в окне с двумя отрицательными элементами: обычным ответом nvm и отдельным крестиком Close. Выбирается единственный отрицательный ответ внутри Main/Buttons; неоднозначные ответы по-прежнему не нажимаются. Закрытие окна проверяется отдельно от возврата обработчика. Если отмена не подтверждена, причина и остановленный статус сохраняются, без ложного сообщения о продолжении фарма.

Автоматический скан один раз для Evolution и один раз для Halo сохраняет исходную кнопку, распознанное подтверждение, положительный/отрицательные ответы, принадлежащие игроку remotes и значения сбросов. Отдельный ограниченный диагност читает байткод UI/client/shared обработчиков и UIAlert/RemoteRequest; модули не исполняются, игровые запросы не отправляются. Результаты находятся в разделе `RETAINED EVOLUTION / HALO SCANS` отчёта F7. Они переживают сбросы, очистку обычного журнала и перезагрузку контроллера в том же клиенте и версии игры. После перезапуска Roblox начинается новая сессия сканирования. Отсутствующий API/тайм-аут записывается, но не останавливает фарм.

Исправления выбора эволюции из 4.0.10, +100 и совместная прокачка базы/стендов сохранены. Новая сборка проверена локально; живая проверка 4.0.11 ещё не выполнена.

В 4.0.10 исправлена обратная сторона защиты от преждевременной эволюции: отдельная доступная или почти накопленная постройка больше не отменяет эволюцию, если проверенный сценарий всего оставшегося каталога с будущими бонусами не укладывается в бюджет быстрого завершения. Цель не привязана к 2 миллионам или другому фиксированному бонусу инвесторов. Пока полного прогноза нет, строительство получает одну отсрочку до 8 секунд на цикл; новые покупки и улучшения не продлевают её. Готовая эволюция проверяется отдельно раз в 2 секунды даже без новых покупок и при выключенном AutoRebirth; ожидаемые квитанции действий по-прежнему блокируют новый сброс.

Подтверждённый FINISH, деньги на весь остаток и оплаченная финальная кнопка сохраняют приоритет, включая проверку перед подтверждением эволюции. Обнадёживающий сценарий будущих бонусов получает ограниченную попытку; новая отрицательная оценка её отменяет. +100 и совместные покупки базы/стендов сохранены. Все локальные тесты проходят; живая проверка 4.0.10 в Roblox ещё не выполнена.

Диагност `dist/inspect-actions.lua` v4 читает текущие обработчики Evolution/Halo и объекты Rebirth/Evolve/Ascend своей базы. Он не открывает меню, не покупает, не вызывает игровые remotes и не выполняет прочитанный байткод. В 4.0.12 основной контроллер использует проверенный прямой путь; отдельный диагност оставлен для ручного разбора обновлений игры.

Покупка базы и улучшения стендов теперь совмещены: сначала резервируется цена следующего Buy, остаток распределяется между всеми доступными стендами, затем запрос постройки и улучшения отправляются параллельно. Каждый ответ подтверждается отдельно. Если денег хватает только на постройку, она покупается без ожидания стендов. Уже зарезервированный бюджет финального каталога сохраняется.

В 4.0.8 исправлены потерянные подтверждения прямых улучшений при смене UpgradeStack: проверяются собственная база, счётчики сброса и точное изменение уровня; смена размера пачки больше не аннулирует выполненную покупку. Запоздавшие подтверждения обрабатываются до новых покупок при продолжении работы, без открытия меню и повторной отправки. Изменение котировки до отправки вызывает перепланирование, а не ошибку прохода.

Прямой режим использует максимальную купленную ступень **+1 / +5 / +25 / +100 / MAX**, включая постоянные уровни, независимо от выбранной в меню. Денег резервируется на всю параллельную пачку. Не купленные ступени не используются. В оценке времени восстановления базы параллельные запросы считаются раундами, а не последовательными кликами по каждому стенду; запас на неизвестное финансирование сохранён.

Регрессия воспроизведена на старой 4.0.7 и проходит на 4.0.8. Отдельно проверены +25, +100, MAX, поздний ответ, смена владельца/цикла, отказ сервера и повторный запуск. Все 53 файла компилируются; полная офлайн-проверка проходит. В последующем логе 4.0.8 наблюдались совместные покупки и прямые rebirth; полного живого подтверждения стабильности нет.

В 4.0.7 исправлена остановка после успешной эволюции: `EVOLUTION_INIT_FAILED / PRESTIGE_BALANCE_INVALID CashSpent`. Игровой `TycoonValues.GetHugeNumber` хранит ноль строкой `"0"`. Читатели расходов, наличных и банка инвесторов теперь распознают её; числовой `0` в log10 по-прежнему означает одну единицу валюты. Прежний отчёт отбрасывал строковые значения и показывал `nil`; теперь он сохраняет исходное значение и тип. Проверены очистка после E3 → E4 и возобновление BOOT → POWERS. Последующий игровой отчёт 4.0.7 подтвердил работу с E4, но обнаружил отдельную остановку подтверждений, исправляемую в 4.0.8.

Модульная переработка приложенного **3.3.3**. Экономические формулы и каталог из 452 позиций сохранены; добавлены независимые наблюдения, ограниченное восстановление чтения, пересмотр стратегии по времени, новое меню, отдельный AntiIdle и публичная загрузка без токена.

**Статус: прямые покупки, параллельные улучшения и rebirth ранее наблюдались в игре; прямые Evolution/Halo добавлены в 4.0.12 и ожидают живой проверки.** Своя база выбирается по `Owner.Value == LocalPlayer`. Текущие Cash, CashSpent, Investors, InvestorsSpent и счётчики сбросов читаются из реплицированных данных в формате log10, подтверждённом кодом клиента. Перед работой сверяются сигнатуры участвующих модулей; неизвестная версия игры блокирует прямой путь. Старый калибруемый GUI-читатель остаётся резервным отдельным адаптером.

В 4.0.6 меню инвесторов больше не открывается ради наблюдений. Награда rebirth рассчитывается по фактическим заработанным и потраченным деньгам, с игровым ограничением учёта потраченных инвесторов. Прогресс и стартовый бонус evolution также рассчитываются из данных базы. Обычный rebirth отправляет один проверенный вызов с сохранением экономических условий и проверяет оба счётчика перерождений. В 4.0.6 Evolution и Halo исполнялись через окно; с 4.0.12 для проверенных обработчиков используется прямой запрос.

После успешного прямого прохода убрана задержка ожидания дохода 0,75 с и лишняя пауза этапа CHECKPOINT. Пустой проход продолжает ждать доход, а пересмотр выгодности сброса сохраняется. Оставшееся окно старого запуска однократно закрывается при старте.

В 4.0.3 обычные улучшения восьми стендов выполняются прямыми `Upgrade(count)` без мыши и открытия Manage. Пачки рассчитываются по 18 997 ценам из текущего клиента, с учётом уже купленных уровней, Halo и открытого UpgradeStack. Перед параллельной отправкой резервируется общий кошелёк. Результат подтверждается точным изменением каждого уровня в той же базе и цикле. Неизвестный результат останавливает проход без повторной покупки. Свежие потоки Income читаются без меню; временное рассогласование уровня и дохода остаётся неизвестным.

Распределение сначала обеспечивает доступное покрытие стендов, затем направляет остаток по наблюдаемой отдаче. Это ограниченная стратегия, не доказательство оптимальности. Сигнатуры и таблицы сверяются с байткодом текущего клиента без его выполнения. Изменение игры или неизвестные эффекты InversionCards останавливают прямые покупки. При загрузке в журнал печатается `[SL HEADLESS]`; повторить отчёт можно через `print(getgenv().SLAutofarm.directReport())`.

В 4.0.4 добавлены прямые **Powers и Buy**. Powers покупаются за инвесторов через подтверждённый обработчик `UpgradePowerLevel(name, 1)`, с сохранением суммарного защищённого банка. Обычный Buy выбирается по 453 внутренним идентификаторам и порядку из текущего Balance, проверяет цену, доступность и оставшиеся использования Remote Buy; запрос `Purchase(true, false)` не тратит forever purchase. Каждая следующая постройка читается заново после подтверждения предыдущей. Прямые покупки не требуют Manage; старое удержание кнопки Buy для них отключено.

Исправлена остановка 4.0.3 на `INVERSION_PRICE_STATE_UNAVAILABLE`: прочитанный InstanceTable создаёт пустую отсоединённую Configuration, пока InversionCards не реплицирована. В текущем Main без инверсии отсутствие папки поэтому означает отсутствие карточных эффектов. Положительная инверсия без данных и неизвестные непустые карточки по-прежнему блокируют расчёт цены.

**Живые результаты:** 4.0.4 подтвердила две покупки UpgradeStack и Windows без GUI. В отрезке 4.0.5 07:39:50–07:41:30 (UTC+5) подтверждены 293 Buy, 234 запроса улучшений и +5097 уровней за 41 параллельную пачку. Старая блокировка Mobile App снялась после обнаружения нового цикла, затем покупка подтвердилась. 4.0.6, ускорение проходов и прямой rebirth ещё требуют нового живого запуска. Полный цикл Halo и работа в свёрнутом Roblox не подтверждены.

В 4.0.2 исправлена ошибка финального прогноза, подтверждённая логом 05:43:58: сценарий будущих бонусов принудительно включал FINISH, обходя `AllowConditionalFinish=false`. Теперь расчёт сначала оплачивает текущую наблюдаемую покупку, затем моделирует будущие бонусы; ×10 применяется только после оплаты. Неизвестный порядок дальнейших открытий остаётся допущением. Сценарий больше не объявляется гарантией финала и не подавляет готовую эволюцию. Действующий FINISH повторно проверяется по свежему доходу и цене; исходный срок не продлевается мелкими покупками. При расхождении запрашивается сравнение продолжения, rebirth и evolution, а не безусловный сброс. Уже оплачиваемый остаток можно завершить без лишнего сброса. Девять регрессионных групп пройдены офлайн; новый проход в Roblox пока не проверен.

По первому игровому логу исправлена повторная проверка исходной Rebirth-кнопки после открытия подтверждения: на этом этапе проверяется соответствующая кнопка диалога, а финансовые ограничения остаются. Сценарий проверен офлайн; успешное перерождение с исправлением в игре ещё не подтверждено.


В 4.0.6 также исправлена остановка `BUY_QUOTE_UNKNOWN`: недоступное чтение Buy откладывает только постройки, а доступные стенды продолжают улучшаться. Контроллер повторяет чтение с паузой и пишет точную причину. Метка `Special` больше не запрещает обычную покупку за деньги — постоянные покупки по-прежнему не отправляются.

Запуск/F6 в прямом режиме больше не требует теста мыши. Тест выполняется перед первым необходимым действием через интерфейс. Ошибка начального отчёта не прерывает создание рабочего цикла; загрузчик различает ошибки скачивания, компиляции и запуска. Новая ошибка загрузки из последнего сообщения пользователя в доступном журнале не обнаружена, поэтому её точная причина пока не подтверждена.

## Запуск без токенов и ключей

Репозиторий `skeetels/glua` публичный. Выполните одну строку:

```lua
local s=type(request)=="function" and request or http_request;assert(type(s)=="function","[SL LOAD] request unavailable");local ok,r=pcall(s,{Url="https://raw.githubusercontent.com/skeetels/glua/main/sell-lemons-controller/dist/controller.lua",Method="GET"});assert(ok,"[SL LOAD] HTTP: "..tostring(r));assert(type(r)=="table" and tonumber(r.StatusCode or r.Status)==200,"[SL LOAD] HTTP status: "..tostring(type(r)=="table" and (r.StatusCode or r.Status)));assert(type(r.Body)=="string","[SL LOAD] missing source");local f,e=loadstring(r.Body);assert(type(f)=="function","[SL LOAD] COMPILE: "..tostring(e));local ran,result=pcall(f);assert(ran,"[SL LOAD] EXEC: "..tostring(result))
```

После появления панели нажмите **Запуск / F6**. F7 — журнал, F8 — скрыть/показать. Кнопка «Закрыть» останавливает контроллер и отключает его подписки. Требуются `request` либо `http_request` и `loadstring`. В текущем Xeno `game:HttpGet` завершился ошибкой до компиляции, поэтому публичный загрузчик использует `request` и проверяет HTTP-статус.

При запуске скачивается одна цельная актуальная сборка, поэтому версии модулей не смешиваются. GitHub-токен и заголовок Authorization не используются. `loader/one-line.lua` содержит ту же строку. `loader/private-loader.lua` сохранён как прежний вариант; для текущего публичного запуска он не нужен.

Новый читатель включается автоматически. В логе появятся `DATA_SOURCE`, `DATA_SNAPSHOT` и, после успешной сверки, `DATA_ENCODING_VERIFIED`. `print(getgenv().SLAutofarm.dataReport())` выводит точечный отчёт; этот же отчёт включён в журнал F7. Прямой адаптер использует подтверждённый формат Cash log10 после сверки схемы игры. Investors сохраняет отдельную сверку.

Для следующего этапа доступен отчёт `print(getgenv().SLAutofarm.inspectData())` после загрузки актуальной панели. Он ограниченно осматривает LocalPlayer, ReplicatedStorage и явно принадлежащий игроку Tycoon. Модули и сетевые объекты перечисляются только по именам; меню не открываются, модули не выполняются, сетевые методы не вызываются. Поля не подключаются к стратегии автоматически. Достижение лимита явно помечается и не означает отсутствия данных.

Диагностику можно запустить отдельно, без перезапуска работающего контроллера. Она делает два снимка с интервалом три секунды и печатает их в лог Roblox:

```lua
loadstring(game:HttpGet("https://raw.githubusercontent.com/skeetels/glua/main/sell-lemons-controller/dist/inspect-data.lua"))()
```

## Запись действительного вызова апгрейда

Для реализации действий без окон нужна сигнатура реального вызова, а не предположение по имени RemoteFunction. Поставьте фарм на паузу, запустите запись и один раз улучшите обычный стенд вручную:

```lua
loadstring(game:HttpGet("https://raw.githubusercontent.com/skeetels/glua/main/sell-lemons-controller/dist/trace-upgrades.lua"))()
```

Инструмент на 90 секунд записывает существующие `FireServer` / `InvokeServer` только внутри базы, чей `Owner.Value` совпадает с LocalPlayer. Он не отправляет и не повторяет игровые запросы. В логе появляются `[SL ACTION] CAPABILITIES`, затем `LISTENING` либо точная причина неподдержки. Локальный тест перехвата не доказывает видимость вызовов игровых скриптов — это устанавливается только по реальному `CALL`. Изменение уровня отмечается отдельно как временная связь, не как доказанный результат конкретного запроса. Поля token/password/secret/cookie скрываются.

`getgenv().SLActionCapture.stop()` завершает запись раньше. При наличии более позднего стороннего перехвата инструмент не перезаписывает его; собственный слой остаётся неактивным сквозным вызовом. Эта диагностика не включена в основной контроллер и не является готовым режимом прямых покупок.

В Xeno пользователя 05:58:20 получено `hookmetamethod=false`, `getnamecallmethod=false`, `decompile=true`, `getscriptbytecode=true`: перехват недоступен. Альтернативный `dist/inspect-client.lua` читает текст до десяти клиентских обработчиков через `decompile`, перечисляет сетевые объекты собственной базы и внутренние имена стендов. Запуск через `request` проверен в живом логе. Однако декомпилятор не поддерживает текущий байткод v12; обновлённая диагностика в этом случае выводит байткод пяти выбранных обработчиков для локального разбора (до 32 КиБ на модуль, до 128 КиБ всего). Ничего не покупает, модули и байткод не запускает, перехваты не устанавливает. Для запуска замените `dist/controller.lua` в строке выше на `dist/inspect-client.lua`. Лог подтвердил внутренний `LemonStand` при отображаемом Lime Stand; имена не заменяются наугад.

## Устройство

`dist/probe-direct-upgrade.lua` — отдельная одноразовая проверка найденной сигнатуры. Версия v3 останавливает прежний контроллер и отправляет по одной попытке `+1` собственным открытым стендам, параллельно и без загрузки игровых модулей. Наличие денег проверяет сервер: это проверка транспорта, а не распределение бюджета. Подтверждением служит изменение реплицированных уровней в той же базе и цикле; неизвестный результат не повторяется. Затем читаются байткоды зависимостей расчёта цены, без выполнения. Это не включённый в основной фарм режим и не новая стратегия постоянных `+1`. Для запуска замените путь файла в публичной строке загрузки; инструмент действительно пытается совершить эти небольшие покупки. Пока он не завершился, основной контроллер запускать не следует.

| Папка | Ответственность |
|---|---|
| `src/core` | Большие числа, прогнозы, политика восстановления, свежесть наблюдений |
| `src/data` | Сохранённый каталог |
| `src/adapters` | Чтение GUI, навигация, проверенные исходником способы ввода |
| `src/strategy` | Выбор улучшений, банк инвесторов, прогноз сброса, маршрут Halo |
| `src/runtime` | Единственный исполнитель, подтверждения, жизненный цикл, наблюдения, AntiIdle |
| `src/ui` | Отображение прогресса и управление |
| `loader` | Публичный запуск одной строкой; прежний приватный загрузчик |
| `tools`, `tests` | Воспроизводимая сборка и офлайн-проверки |

Каждый модуль — обычная функция инициализации с явным контекстом зависимостей. В рабочем коде нет подмены окружения через `setfenv`, динамического скачивания отдельных модулей или обфускации. `manifest.json` задаёт порядок. `dist/controller.lua` генерируется; редактируют исходники в `src/`. Полный переход от общей структуры контекста к изолированным сервисным интерфейсам ещё не выполнен: существующая стратегия намеренно перенесена без переписывания всех её формул за один раз.

## Изменения поведения

- Повторное чтение неизменившегося скрытого текста не обновляет его возраст. Изменение скрытых значений может запросить проверку, но не разрешает сброс само по себе.
- Подтверждённый сброс аннулирует предыдущие наблюдения.
- В PREPARE стратегия пересматривается не реже 45 секунд при доступном исполнителе; число покупок +1 не может бесконечно откладывать проверку. Это интервал **проверки**, а не фиксированный множитель для rebirth. Незавершённый проход сохраняется. FINISH, восстановление после сброса и незавершённые транзакции сохраняют свои ограничения.
- Пять конкретных ошибок временной недоступности меню/данных допускают до четырёх повторов с задержкой 1/2/4/8 секунд. Подтверждённый прогресс сбрасывает счётчик. Ручная пауза отменяет восстановление. Ошибка покупки, неизвестный ответ и ошибка выполнения автоматически не повторяются.
- AntiIdle подписывается на `Player.Idled` и пробует отдельную пару событий VirtualUser. Он не отключает обработчики игры и не работает одновременно с экономической транзакцией. `INPUT_SENT_UNVERIFIED` означает только вызов API; отсутствие AFK-кика не подтверждено.
- Панель имеет масштабирование, прокрутку длинного прогресса, перетаскивание и отдельный переключатель AntiIdle.

## Проверка и ограничения

```powershell
python tools/build.py
python tools/build.py --check
python tools/test.py --luau-dir C:\path\to\luau
```

Нужны Python 3.10+ и `luau` / `luau-compile` из официального [Luau](https://github.com/luau-lang/luau). Тесты не скачивают и не исполняют сторонние хабы. `dist/integrity.json` содержит SHA-256 сборки и исходников для контроля воспроизводимости, а не отдельную цифровую подпись.

Проверяются свежесть/аннулирование данных, сохранение прохода при временном сбое, ручная отмена, запрет повторов неоднозначной покупки, своевременный пересмотр, ограничения FINISH, загрузчик с поддельными ответами HTTP, инициализация/диагностика/отключение подписок на минимальном макете движка. Макет не имитирует серверные покупки, физику, свёрнутое окно или реальное время AFK. Полная статическая типизация старой динамической стратегии не пройдена; отдельно выполнена именно компиляция Luau.

Актуальные результаты проверок и подтверждённые строки игрового лога перечислены в `VALIDATION.md`. В интеграционной проверке используются заглушки движка. В рабочей сборке три цикла: единственный исполнитель экономических действий, независимый наблюдатель без действий и обновление панели.

До заявления о полной готовности остаются живые проверки:

1. Проверка актуальной публичной сборки через `request` в вашей Lua-среде.
2. Получение нового GUI и работа кнопок; покупка → появление реального уведомления → подтверждение → продолжение прохода.
3. Сценарий долгих +1, оценка выгоды rebirth по свежему доходу и времени восстановления, сохранение эволюции.
4. Работа в фоне и проверка AntiIdle в течение реального периода простоя.
5. Подтверждение прямых Evolution/Halo в текущем клиенте: точные счётчики, закрытые вкладки и возобновление строительства после сброса.

Ссылка Acro из запроса была прочитана как текст, без запуска. Она перенаправляет на GitHub-загрузчик, затем Luarmor; открытой логики Sell Lemons в этих двух файлах нет. Она не доказывает конкретный механизм или совместимость этого проекта. Обход ключей Acro в проект не включён.

В 4.0.5 счётчики реального сброса отделяют старый цикл от нового. Неподтверждённые запросы прошлого цикла снимаются с блокировки, но не засчитываются успешными. Внутри того же цикла блокировка остаётся; уже выполняющийся запрос не освобождается. После rebirth сохраняется учёт потраченных инвесторов. Начальный отчёт печатается отдельными строками, чтобы Roblox не обрезал сведения Buy/Powers.
